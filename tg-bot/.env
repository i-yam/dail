# Telegram bot token from @BotFather (required).
BOT_TOKEN=8576447141:AAHXuIp5lr2LpVxQuJcnvE6xXm-KW3n4sxA

# URL of the model /detect service.
# Default is the mock running on localhost:8000. Change to point at the real
# detector when it's ready — the bot needs no code changes.
MODEL_URL=http://localhost:8000

# How long to wait for the model to reply before returning an error verdict.
MODEL_TIMEOUT_S=20

# Optional: comma-separated Telegram user IDs who get admin-only commands.
# (Admin commands not implemented yet — placeholder for /monitor on|off.)
ADMIN_USER_IDS=

AUTO_TRANSLATE=1
